node_modules
dist
.env.local
.DS_Store
*.local
# .env (sin punto extra) SÍ se commitea a propósito — solo tiene defaults
# sin secretos (ver comentario en ese archivo). Los secretos reales viven
# en .env.local, cubierto arriba y también por *.local.

# Informes exportados (generados desde el .md con scripts/md-to-*)
docs/export/

# Playwright E2E — nunca commitear resultados de corridas ni el token de sesión
test-results/
playwright-report/
blob-report/
playwright/.cache/
e2e/.auth/
